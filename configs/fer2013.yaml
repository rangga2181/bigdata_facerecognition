defaults:
  - default

dataset:
  name: fer2013
  root: datasets/fer2013
  image_size: 224
  grayscale: false   # convert grayscale to 3-channel RGB

training:
  epochs: 60
  batch_size: 64
  lr: 0.0005
